@page
@model BackendLab01.Pages.QuizList
@{
    ViewData["Title"] = "Quiz List";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Quiz List</h1>

@foreach (var quiz in Model.Quizzes)
{
    <a asp-page="Item" asp-route-quizId="@quiz.Id" asp-route-itemId="1">@quiz.Title</a>
}
